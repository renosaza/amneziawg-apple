// Developer.xcconfig

// You Apple developer account's Team ID
DEVELOPMENT_TEAM = <team_id>

// The bundle identifier of the apps. Register the macOS app ID and its
// Network Extension capability at developer.apple.com.
APP_ID_IOS = <app_id>
APP_ID_MACOS = com.renosaza.amneziawg

// Leave Sparkle disabled for normal development builds. The manual release
// candidate workflow overrides both values without storing them in the project.
SPARKLE_FEED_URL =
SPARKLE_PUBLIC_ED_KEY =
